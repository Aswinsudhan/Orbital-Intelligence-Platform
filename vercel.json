{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/oip run build",
  "outputDirectory": "artifacts/oip/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
